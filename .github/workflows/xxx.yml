on:
  push:
    branches:
      - main

jobs:
  xxx_yyy:
    runs-on: ubuntu-latest
    steps:
      
      - name: checkout
        uses: actions/checkout@v2
      
      - name: actions-ecosystem/action-get-merged-pull-request
        uses: actions-ecosystem/action-get-merged-pull-request@v1
        id: get-merged-pull-request
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}



      - name: my-stepB
        if: steps.get-merged-pull-request.outputs.labels != 'piyo' && steps.get-merged-pull-request.outputs.labels != ''
        run: |
          echo "Hello World!"
          echo '${{ toJSON(steps.get-merged-pull-request.outputs) }}'


